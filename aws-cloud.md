IAM access to a service
    1. Make policy that gives access to a resource or service
    2. Add that policy to a user group
    3. Add the IAM user to the previously created Group
    4. example: lightsail access ploicy a
